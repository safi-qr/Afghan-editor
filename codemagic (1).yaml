
workflows:
  android-workflow:
    name: Build Afghan Editor
    max_build_duration: 10
    environment:
      vars:
        PACKAGE_NAME: "com.afghan.editor"
    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/*.apk
